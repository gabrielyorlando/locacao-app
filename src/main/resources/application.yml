server:
  port: 9090
spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/locacao-app
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  data:
    web:
      pageable:
        default-page-size: 10

springdoc:
  swagger-ui:
    path: /doc

jwt:
  private:
    key: classpath:app.key
  public:
    key: classpath:app.pub
